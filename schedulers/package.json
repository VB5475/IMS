{
  "name": "ims-schedulers",
  "version": "1.0.0",
  "private": true,
  "description": "Standalone scheduled jobs for IMS. Fully self-contained — its own dependencies, its own .env — so it can be deployed and run independently of the frontend app, triggered by an external scheduler (cron, Windows Task Scheduler, CI).",
  "type": "module",
  "scripts": {
    "sync:employees": "node sync-employees.js"
  },
  "dependencies": {
    "axios": "^1.16.0",
    "dotenv": "^16.4.5",
    "mssql": "^11.0.1",
    "pg": "^8.13.1"
  }
}

// uiStrings.js — Common UI labels shared across all modules.
// Modules re-export these (and may override) via their own constants.js.
// Usage: import { ENTRY_FORM_LABEL } from "../../constants/uiStrings";

export const ENTRY_FORM_LABEL = "Entry Form";
